install react-dom
firebase
install material-ui/core
install material-ui/icons
install react-router-dom
import browserrouter as router,switch,route
router(switch(route))
Links are used in React not tha 'a' tag as 'a' tag refreshes the pages which is not instant but links are instant

Material ui gives a class of MuiSvgIcon-root to every icon